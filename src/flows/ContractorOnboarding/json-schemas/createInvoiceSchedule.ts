// The create-invoice-schedule schema is shared with the standalone InvoiceSchedule flow and
// lives in src/common/invoice-schedules. Re-exported here so existing imports keep resolving.
export { createInvoiceScheduleSchema } from '@/src/common/invoice-schedules/json-schema';
