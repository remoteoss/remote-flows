{
  "version": 2,
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "github": {
    "enabled": false
  },
  "installCommand": "node scripts/set-commit-hash.js && npm install",
  "buildCommand": "cd .. && npm ci --ignore-scripts && npm run build && cd example && npm run build"
}
