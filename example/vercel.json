{
  "version": 2,
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "github": {
    "enabled": false
  },
  "installCommand": "node scripts/set-commit-hash.js && pnpm install --no-frozen-lockfile 2>&1 | while IFS= read -r l; do printf '%s %s\n' \"$(date +%T)\" \"$l\"; done"
}
